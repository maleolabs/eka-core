# Go build artifacts
/dist/
*.test
*.out

# Local EKA workspace registration identity (dev-time, machine-specific)
/eka.yaml

# EKA runtime transient state (sync staging, never committed)
**/exchange/.snapshots-tmp/
**/exchange/.snapshots-old/
